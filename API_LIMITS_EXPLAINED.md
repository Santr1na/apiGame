# ⚠️ Реальные лимиты API и как загрузить максимум игр

## 📊 Теоретическое количество игр vs Реальность

### Что заявлено в API:
| API | Заявлено игр | Реальность |
|-----|--------------|------------|
| RAWG | 350,000+ | ✅ Доступно через API |
| Giant Bomb | 40,000+ | ✅ Доступно через API |
| Steam | 50,000+ | ✅ Доступно через API |
| TheGamesDB | 350,000+ | ⚠️ Ограничен поиском |

### Почему не получается загрузить все 790,000+ игр?

## 🚧 Ограничения бесплатных API

### 1. RAWG API
**Лимиты:**
- ✅ 20,000 запросов в день
- ✅ 40 игр на страницу
- ✅ Максимум: 20,000 × 40 = 800,000 игр теоретически

**Реальность:**
- 🔴 API возвращает максимум ~10,000 страниц
- 🔴 После определенной страницы начинает возвращать пустые результаты
- ✅ **Реально можно загрузить: ~40,000 игр**

**Решение:**
```javascript
const maxPages = 1000; // ~40,000 игр за 1000 запросов
// Займет: 1000 сек × 1 сек задержка = ~17 минут
```

### 2. Giant Bomb API
**Лимиты:**
- ⚠️ 200 запросов в день (бесплатный тариф)
- ✅ 100 игр на запрос
- ✅ Максимум: 200 × 100 = 20,000 игр в день

**Реальность:**
- 🔴 Нужно несколько дней для загрузки всех 40,000 игр
- ✅ **За 1 день можно загрузить: ~20,000 игр**
- ✅ **За 2 дня: ~40,000 игр (все доступные)**

**Решение:**
```javascript
const maxRequests = 400; // Для 2 дней загрузки
// День 1: 200 запросов = 20,000 игр
// День 2: 200 запросов = еще 20,000 игр
```

### 3. Steam API
**Лимиты:**
- ✅ Нет лимитов на GetAppList
- ✅ Возвращает ВСЕ приложения сразу (~150,000)

**Реальность:**
- ⚠️ Не все приложения - это игры (DLC, софт, демо)
- ✅ **Реально игр: ~50,000-70,000**

**Решение:**
```javascript
// Загружаем все приложения и фильтруем
const validApps = steamApps.filter(app => 
  app.name && app.name.length > 2
);
// Получаем ~50,000+ игр
```

### 4. TheGamesDB API
**Лимиты:**
- ✅ 1,000 запросов в час
- ⚠️ Нет endpoint для "всех игр"
- ⚠️ Только поиск по названию

**Реальность:**
- 🔴 Нужно делать множество поисковых запросов
- 🔴 Много дубликатов в результатах
- ✅ **Реально можно загрузить: ~10,000-20,000 игр**

**Решение:**
```javascript
// Поиск по всем буквам и цифрам
const searchTerms = 'abcdefghijklmnopqrstuvwxyz0123456789'.split('');
// 36 терминов × 100 страниц × 10 игр = ~36,000 запросов
// Займет: 36 часов при лимите 1000/час
```

---

## 🎯 Реалистичные цифры загрузки

### За 1 день (24 часа):
```bash
npm run populate:all
```

| Источник | Игр | Запросов | Время |
|----------|-----|----------|-------|
| RAWG | ~40,000 | 1,000 | ~17 мин |
| Giant Bomb | ~20,000 | 200 | ~10 мин |
| Steam | ~50,000 | 1 | ~5 мин |
| TheGamesDB | ~10,000 | 1,000 | ~30 мин |
| **ИТОГО** | **~120,000** | **2,201** | **~1 час** |

### За 2 дня:
| Источник | Игр | Запросов | Время |
|----------|-----|----------|-------|
| RAWG | ~40,000 | 1,000 | ~17 мин |
| Giant Bomb | ~40,000 | 400 | ~20 мин (2 дня) |
| Steam | ~50,000 | 1 | ~5 мин |
| TheGamesDB | ~20,000 | 2,000 | ~60 мин (2 дня) |
| **ИТОГО** | **~150,000** | **3,401** | **~2 часа** |

### За неделю (максимум):
| Источник | Игр | Запросов |
|----------|-----|----------|
| RAWG | ~40,000 | 1,000 |
| Giant Bomb | ~40,000 | 1,400 (7 дней) |
| Steam | ~50,000 | 1 |
| TheGamesDB | ~50,000 | 7,000 (7 дней) |
| **ИТОГО** | **~180,000** | **9,401** |

---

## 💡 Стратегии загрузки

### Стратегия 1: Быстрая (рекомендуется)
```bash
npm run populate:all
```
- ⏱️ Время: 1-2 часа
- 📊 Игр: ~120,000
- ✅ Достаточно для большинства проектов

### Стратегия 2: Поэтапная (максимум игр)
```bash
# День 1
npm run populate:all

# День 2 (продолжить Giant Bomb и TheGamesDB)
npm run populate:all

# День 3-7 (опционально)
npm run populate:all
```
- ⏱️ Время: 7 дней
- 📊 Игр: ~180,000
- ✅ Максимальное покрытие

### Стратегия 3: Платные API (без лимитов)
Если нужны ВСЕ игры, рассмотрите платные тарифы:

**RAWG Pro:**
- 💰 $10-50/месяц
- ✅ 100,000 запросов/день
- ✅ Можно загрузить все 350,000+ игр

**Giant Bomb Premium:**
- 💰 $5/месяц
- ✅ Без лимитов запросов
- ✅ Можно загрузить все 40,000+ игр

---

## 🔧 Настройка лимитов в коде

Отредактируйте [`populate-all-games.js`](populate-all-games.js):

```javascript
// RAWG - строка 32
const maxPages = 1000; // Увеличьте до 2000 для большего покрытия

// Giant Bomb - строка 105
const maxRequests = 400; // Для 2 дней загрузки

// TheGamesDB - строка 238
while (hasMore && page <= 100) { // Увеличьте до 200
```

---

## 📈 Мониторинг прогресса

### Проверить сколько игр загружено:
```bash
node -e "require('./db-setup').initDatabase().then(() => require('./db-setup').getGamesCount()).then(c => console.log('Games:', c))"
```

### Проверить по источникам:
```bash
node -e "require('./db-setup').initDatabase().then(() => require('./db-setup').getCountBySource()).then(c => console.log(c))"
```

---

## ❓ FAQ

### Можно ли обойти лимиты API?
❌ Нет, это нарушает условия использования API

### Что делать если лимит исчерпан?
✅ Подождите 24 часа и продолжите загрузку

### Нужно ли загружать все игры?
❌ Для большинства проектов достаточно 10,000-50,000 игр

### Как ускорить загрузку?
✅ Используйте платные API тарифы
✅ Запускайте скрипт на сервере (не прерывается)

---

## 🎯 Рекомендации

**Для малого проекта (тестирование):**
```bash
npm run populate  # ~800 игр, 5-10 минут
```

**Для среднего проекта (продакшн):**
```bash
npm run populate:all  # ~120,000 игр, 1-2 часа
```

**Для крупного проекта (максимум):**
```bash
# Запускайте несколько дней подряд
npm run populate:all  # День 1: ~120,000
npm run populate:all  # День 2: +30,000
npm run populate:all  # День 3-7: +30,000
# Итого: ~180,000 игр
```

---

## ✅ Итоговая таблица

| Вариант | Игр | Время | Дней | Команда |
|---------|-----|-------|------|---------|
| Быстрый | ~800 | 10 мин | 1 | `npm run populate` |
| Стандарт | ~120,000 | 1-2 часа | 1 | `npm run populate:all` |
| Максимум | ~180,000 | 7-14 часов | 7 | `npm run populate:all` (×7) |
| Платный | ~790,000 | 2-3 дня | 3 | Платные API |

**Вывод:** Реально можно загрузить **120,000-180,000 игр** бесплатно, что покрывает большинство популярных игр!
